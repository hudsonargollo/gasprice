VPS PM2 SETUP AND APPLICATION START
===================================

You're currently on your VPS and the build completed successfully!
PM2 is not installed, so let's install it and start the application.

RUN THESE COMMANDS ON YOUR VPS:
==============================

1. Install PM2 globally:
   npm install -g pm2

2. Start the application with PM2:
   pm2 start dist/index.js --name fuelprice-backend

3. Save PM2 configuration (auto-restart on reboot):
   pm2 save
   pm2 startup

4. Check application status:
   pm2 status

5. Test the application:
   curl https://pricepro.clubemkt.digital/health

6. Test factory provisioning endpoints:
   TOKEN=$(curl -s -X POST https://pricepro.clubemkt.digital/api/auth/login \
     -H "Content-Type: application/json" \
     -d '{"username":"admin","password":"admin123"}' | \
     grep -o '"token":"[^"]*"' | cut -d'"' -f4)
   
   echo "Token: $TOKEN"
   
   curl -H "Authorization: Bearer $TOKEN" \
     https://pricepro.clubemkt.digital/api/factory/wizard/steps
   
   curl -X POST -H "Authorization: Bearer $TOKEN" \
     -H "Content-Type: application/json" \
     -d '{"mikrotikSerial":"TEST123","huiduSerial":"TEST456"}' \
     https://pricepro.clubemkt.digital/api/factory/test-devices

ALTERNATIVE (if PM2 installation fails):
========================================

If PM2 installation doesn't work, you can start the app directly:

1. Start the application in background:
   nohup node dist/index.js > app.log 2>&1 &

2. Check if it's running:
   ps aux | grep node

3. Test the application:
   curl https://pricepro.clubemkt.digital/health

WHAT SHOULD HAPPEN:
==================

✅ PM2 installs successfully
✅ Application starts on port 3000
✅ Health endpoint returns {"status":"OK"}
✅ Factory provisioning endpoints return data (not "route not found")
✅ Mobile app "Testar dispositivos" will work

CURRENT STATUS:
==============
✅ Code updated from GitHub
✅ Dependencies installed  
✅ TypeScript compiled successfully
⏳ Need to install PM2 and start application

Run the PM2 commands above to complete the deployment!